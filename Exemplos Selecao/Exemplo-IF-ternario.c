#include <stdio.h>

int main()
{
    int a = 10, b = 20;
    int maior = (a > b) ? a : b; // Se a > b for verdadeira, "maior" recebe "a", caso contrário recebe "b"
    printf("O maior valor é %d", maior);


    return 0;
}

    
